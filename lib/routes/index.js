const makeAuthRoute = require('./auth')
const makeCallbackRoute = require('./callback')
const makeHomeRoute = require('./home')

module.exports = {
  makeAuthRoute,
  makeCallbackRoute,
  makeHomeRoute
}
